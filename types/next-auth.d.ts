// Deprecated — NextAuth fue reemplazado por Supabase Auth (2026-04-18).
//
// Ya no declaramos módulo "next-auth". El contexto de auth ahora vive en
// `server/supabase/auth-context.ts` y se tipa desde `types/supabase.ts`
// (tipos generados por Supabase).
//
// Este archivo se mantiene vacío solo porque no se puede eliminar desde
// esta sesión. Bórralo manualmente cuando puedas.

export {}
